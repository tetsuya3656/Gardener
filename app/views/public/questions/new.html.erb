<h1>質問・お悩み</h1>

<%= form_with model :@question do |f| %>
  <h4>画像</h4>
  <%= f.file_field :image, accept: "image/*" %>
  <h4>タイトル</h4>
  <%= f.text_field :title %>
  <h4>質問内容</h4>
  <%= f.text_area :body %>

  <%= f.submit "投稿" %>
<% end %> 
