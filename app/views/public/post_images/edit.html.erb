<h1>投稿編集</h1>

<%= form_with model :@post_image do |f| %>
  <h4>画像</h4>
    <%= f.file_field :image, accept: "image/*" %>
  <h4>説明文</h4>
    <%= f.text_area :caption %>
  <%= f.submit "投稿内容を編集" %>
<% end %>

